<!DOCTYPE html>
<html>
<head>
  <title>Providers list</title>
</head>
<body>
<h2>Providers list:</h2>
<table border="1">
  <tr>
    <th>Id</th>
    <th>Device</th>
    <th>Class</th>
    <th></th>
  </tr>

  <% @providers.each do |provider| %>
      <tr>
        <td><%= provider.id %></td>
        <td><%= provider.name %></td>
        <td><%= provider.class.name %></td>
        <td><%= link_to("details", :action => "edit", :id => provider.id) %></td>
      </tr>
  <% end %>
</table>

<br>
<%= form_tag("/providers/new", :method => "get") do %>
    <%= select(:provider, :type, @provider_list) %>
    <%= text_field(:provider, :name) %>
    <%= submit_tag("Add provider") %>
<% end %>

</body>
</html>