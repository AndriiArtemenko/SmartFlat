<!DOCTYPE html>
<html>
<head>
  <title><%= @report.name %></title>
</head>
<body>
<h2><%= @report.name %>(<%= @report.id %>):</h2>

<div  style="width: 50%">
  <table class="table table-hover">
    <tr>
      <td>Source</td>
      <td><%= @report.device==nil ? "None" : @report.device.name %></td>
    </tr>
    <tr>
      <td>Type</td>
      <td><%= @report.message_template == nil ? "None" : @report.message_template.type %></td>
    </tr>
    <tr>
      <td>Address</td>
      <td><%= @report.message_template == nil ? "None" : @report.message_template.address %></td>
    </tr>
    <tr>
      <td>Subject</td>
      <td><%= @report.message_template == nil ? "None" : @report.message_template.subject %></td>
    </tr>
    <tr>
      <td>Body</td>
      <td><%= @report.message_template == nil ? "None" : @report.message_template.body %></td>
    </tr>
  </table>

</div>
</body>
</html>

